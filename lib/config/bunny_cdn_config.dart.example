/// Bunny CDN Configuration Template
///
/// Copy this file to bunny_cdn_config.dart and fill in your credentials
///
/// IMPORTANT: Never commit bunny_cdn_config.dart with real credentials!
///
/// Storage Zone: kins-app (UK)
/// Dashboard: Storage → kins-app → FTP & API access
class BunnyCDNConfig {
  // Storage Zone Name
  static const String storageZoneName = 'kins-app';

  // API Key = Storage Zone "Password" (FTP & API access → Password, NOT Read-only password)
  static const String apiKey = 'YOUR_STORAGE_ZONE_PASSWORD';

  // Storage API hostname (region: UK)
  static const String cdnHostname = 'uk.storage.bunnycdn.com';
}
